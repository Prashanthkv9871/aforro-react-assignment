export enum PageRoutes {
    Dashboard = "/",
    Users = "/users",
    Leaderboard = "/leaderboard",
    Orders = "/orders",
    Products = "/products",
    SalesReport = "/sales-report",
    Messages = "/messages",
    Settings = "/settings"
}